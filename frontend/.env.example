# Base origin of the backend API, with no trailing slash and no `/api` suffix
# (e.g. https://research-pilot-api.onrender.com). Leave unset for local dev —
# Vite's dev proxy (vite.config.ts) forwards relative /api/* calls to the
# local backend instead.
VITE_API_BASE_URL=
